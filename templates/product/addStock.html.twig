{% extends 'base.html.twig' %}

{% block title %}Ajouter le stock
{% endblock %}

{% block body %}
	<div class="container"><br><br>
       

        <h1 class="text-primary text-center mt-4">Ajouter le Stock : {{product.name}}</h1>
         {% include "layouts/_flash_message.html.twig" %}
		{{ form_start(form)}}
		<label for="" class="text-primary mt-4">Ajouter le stock</label>
		{{ form_widget(form.qte,{attr:{'class': 'form form-control mt-2'}}) }}
        <input type="submit" value="Ajouter le stock" class="btn btn-outline-primary mt-4">
        <a href="{{ path('app_product_index') }}" class="btn btn-outline-primary mt-4">Retour à la liste des produits</a>
       

		{{ form_end(form) }}


	</div>

{% endblock %}
