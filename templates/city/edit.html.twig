{% extends 'base.html.twig' %}

{% block title %}Edit City
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="text-primary">Modifier Ville</h1>

		{{ include('city/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

		<a href="{{ path('app_city_index') }}" class="btn btn-outline-primary mt-2">Retour à la liste</a>

		{{ include('city/_delete_form.html.twig') }}
	</div>
{% endblock %}
