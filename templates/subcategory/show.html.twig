{% extends 'base.html.twig' %}

{% block title %}Subcategory{% endblock %}

{% block body %}
<div class="container"><br><br>
        <h1 class="text-center mt-4 text-primary">Sous catégorie</h1><br>
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subcategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subcategory.name }}</td>
                <td>
                    <a class="btn btn-outline-primary" href="{{ path('app_subcategory_show', {'id': subcategory.id}) }}">show</a>
                    <a class="btn btn-outline-danger" href="{{ path('app_subcategory_edit', {'id': subcategory.id}) }}">edit</a>
                
                </td>
                
            </tr>
           
        </tbody>
    </table>

 

    {{ include('subcategory/_delete_form.html.twig') }}
</div>
{% endblock %}
